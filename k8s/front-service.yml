apiVersion: v1
kind: Service
metadata:
  labels:
    app: devops-front
  name: devops-front
spec:
  type: NodePort
  ports:
  - port: 3000
    protocol: TCP
    targetPort: 3000
  selector:
    app: devops-front