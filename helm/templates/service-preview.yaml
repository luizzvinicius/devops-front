{{- if eq .Values.strategy "bluegreen" }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-preview
spec:
  selector:
    app: {{ .Release.Name }}
  ports:
    - port: 80
      targetPort: 3000
{{- end }}